{
  "name": "mr-image-mistrall-fork",
  "version": "0.0.3",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-copy": "^0.8.0",
    "grunt-contrib-uglify": "^0.8.1",
    "grunt-contrib-cssmin": "^0.12.2"
  },
  "scripts" : {
    "build": "node -e \"require('grunt').tasks(['default']);\""
  }
}
